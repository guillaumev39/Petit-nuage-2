Base surface for products, reassurance blocks, forms.

```jsx
<Card hoverLift>…product…</Card>
<Card variant="soft">Livraison offerte dès 80 €</Card>
```

Variants: `shadow` (default), `outline`, `soft` (écru), `jouy` (pale blue).
