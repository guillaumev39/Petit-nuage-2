Round icon-only button for cart, favorites, close, carousel arrows. Always pass `label`.

```jsx
<IconButton label="Ajouter aux favoris"><Heart size={20} /></IconButton>
```

Variants: `soft` (sky tint, default), `ghost`, `solid`.
