Pill-shaped action button for all CTAs; labels are French, sentence case ("Ajouter au panier").

```jsx
<Button variant="primary" size="lg">Découvrir la collection</Button>
<Button variant="outline" icon={<HeartIcon />}>Ajouter aux favoris</Button>
```

Variants: `primary` (solid sky), `secondary` (soft sky tint), `outline` (hairline), `ghost`. Sizes sm/md/lg (34/44/52px). Hover darkens; never uppercase.
